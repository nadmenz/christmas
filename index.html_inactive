<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Meine Test-Webseite</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f0f0f0;
            color: #333;
            text-align: center;
        }

        header {
            background-color: #4CAF50;
            color: white;
            padding: 20px;
        }

        section {
            padding: 40px;
            background-color: #fff;
            margin: 20px 0;
            border-radius: 8px;
        }

        footer {
            background-color: #333;
            color: white;
            padding: 10px;
            position: fixed;
            bottom: 0;
            width: 100%;
        }

        button {
            background-color: #007BFF;
            color: white;
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }

        button:hover {
            background-color: #0056b3;
        }

        .content {
            opacity: 0;
            transition: opacity 2s ease-in;
        }

        .content.visible {
            opacity: 1;
        }

        /* Scroll animation */
        .scroll-message {
            font-size: 1.2em;
            color: #ff6600;
        }
    </style>
</head>
<body>

    <header>
        <h1>Willkommen auf meiner Test-Webseite!</h1>
        <p>Dies ist eine kleine, einfache Webseite, um die Grundfunktionen von HTML und CSS zu zeigen.</p>
    </header>

    <section>
        <h2>Abschnitt 1: Scroll-Ereignis</h2>
        <p class="scroll-message">Scrollen, um den Text anzuzeigen...</p>
        <div class="content" id="scroll-content">
            <p>Dieser Text erscheint, wenn du nach unten scrollst. Cool, oder?</p>
        </div>
    </section>

    <section>
        <h2>Abschnitt 2: Einfache Interaktivität</h2>
        <p>Drücke den Button, um eine Nachricht anzuzeigen.</p>
        <button onclick="showMessage()">Klicke mich!</button>
        <p id="message" style="display: none;">Danke, dass du geklickt hast! 😊</p>
    </section>

    <footer>
        <p>&copy; 2025 Meine Test-Webseite</p>
    </footer>

    <script>
        // Funktion, um den Text beim Klicken auf den Button anzuzeigen
        function showMessage() {
            document.getElementById('message').style.display = 'block';
        }

        // Scroll-Ereignis, um Text anzuzeigen
        window.addEventListener('scroll', function() {
            var scrollPosition = window.scrollY;
            var content = document.getElementById('scroll-content');
            if (scrollPosition > 200) { // Wenn du 200px nach unten scrollst
                content.classList.add('visible');
            }
        });
    </script>

</body>
</html>
